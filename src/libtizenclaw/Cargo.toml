[package]
name = "libtizenclaw-client"
version = "1.0.0"
edition = "2021"
description = "TizenClaw C API client library — Rust internals with C FFI"

[lib]
name = "tizenclaw_client"
crate-type = ["cdylib", "rlib"]

[dependencies]
serde_json = "1"
log = "0.4"
libc = "0.2"
