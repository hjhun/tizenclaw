[package]
name = "libtizenclaw"
version = "1.0.0"
edition = "2021"
description = "TizenClaw C API library — Rust internals with C FFI"

[lib]
name = "tizenclaw"
crate-type = ["cdylib", "rlib"]

[dependencies]
serde_json = "1"
log = "0.4"
libc = "0.2"
