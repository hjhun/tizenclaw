[package]
name = "tizenclaw-cli"
version = "1.0.0"
edition = "2021"

[dependencies]
tizen-sys = { path = "../tizen-sys" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
libc = "0.2"
