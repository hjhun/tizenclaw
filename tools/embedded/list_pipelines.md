# list_pipelines

List all configured pipelines with their names, triggers, and step counts.

**Category**: pipeline

## Parameters

No parameters required.

## Schema

```json
{
  "name": "list_pipelines",
  "description": "List all configured pipelines with their names, triggers, and step counts.",
  "inputSchema": {
    "type": "object",
    "properties": {},
    "required": []
  }
}
```
