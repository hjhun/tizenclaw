{
    "name": "web_search",
    "version": "2.0",
    "description": "Search the web using Naver (default) or Google. Returns top search results with titles, snippets, and URLs. Daily usage is rate-limited.",
    "entrypoint": "python3 web_search.py",
    "dependencies": [],
    "parameters": {
        "type": "object",
        "properties": {
            "query": {
                "type": "string",
                "description": "Search query string"
            },
            "engine": {
                "type": "string",
                "enum": [
                    "naver",
                    "google"
                ],
                "description": "Search engine to use. Default is naver. Use google only if user explicitly requests it."
            }
        },
        "required": [
            "query"
        ]
    }
}