Implement storage audit persistence contracts

Add audit log query APIs and SQLite bootstrap settings for WAL mode.
Extend the session store with prompt-compatible load and append helpers,
atomic compacted snapshot writes, and on-disk session listing.

Add memory key listing and a vector embedding persistence/search layer.
Cover the new storage contracts with unit tests and validate them with
the host deploy and host test scripts.
