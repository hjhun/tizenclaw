Implement prompt and role runtime delegation

Align role loading with the deployed agent configuration and
wire runtime role metadata into delegated sessions.

- Accept both `agents` and legacy `roles` schemas
- Preserve role type, delegation, and loop budget metadata
- Execute `run_supervisor` with real delegated sessions
- Package `agent_roles.json` and `system_prompt.txt`
- Verify deploy, managed tests, and service restart on x86_64
