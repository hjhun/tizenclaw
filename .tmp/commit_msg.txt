Migrate internal dashboard tracking to .dev

Update agent rules and structure docs to point workflow tracking
from `.dev_note` to `.dev`.

Remove the legacy `.dev_note` dashboard and archived design docs,
and keep the touched Rust files aligned with formatting cleanup.
