Implement unified tool registry

Add a manifest-driven tools subsystem under rust/crates/tclaw-tools.
Split metadata, registry composition, built-in handlers, and
permission-aware execution into focused modules.

Expose representative built-in tools for file, shell, network,
task, worker, cron, and LSP registry flows.
Add plugin tool manifests and preserve MCP input schemas so
plugin and bridged tools can join the unified registry.

Record host build, deploy, and review evidence in DASHBOARD.md.
